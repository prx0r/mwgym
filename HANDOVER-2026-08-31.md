# MWGym Handover — 2026-08-31

## Current State

### What Works
- **runtime-letta**: UP on :3000 (real Letta Agent SDK)
- **LiveLLM**: UP on :3847 (real pricing data)
- **Harbor adapter**: Working (MockHarbor for tests, real adapter for production)
- **Telemetry**: ModelCallRecord, ResourceSpend, validation
- **BudgetLedger**: Cost tracking
- **AssetProfile**: Beta posteriors for Thompson sampling
- **StackOracle**: Thompson allocator (68% win rate on YGO synthetic)
- **ComputeWallet**: Multi-source budget tracking
- **LabBridge**: SQLite degraded projection

### What's NOT Working
- **Letta timing**: Runs take 35s-180s, HTTP timeout issues
- **Harbor CLI**: MockHarbor only, `harbor run` not called
- **HydraDB**: Docker running but disk full
- **Real submissions**: All synthetic

### Key Results
- **Arm A (direct)**: 9/10 pass on 10 real coding tasks
- **Arm B/C (Letta)**: Timeout issues, need 180s+ timeout
- **YGO synthetic**: 68% win rate with Thompson (meaningless against real opponents)

## What the Spec Says (HARBOR-PLAN.md)

The authoritative spec says:
1. **Stop building synthetic Worlds** (YGO)
2. **Use Harbor as the World/evaluation substrate**
3. **Real repositories, real briefs, real rubrics, real artifacts**
4. **Initial task family: technical_submission**
5. **Never use MockHarbor in production**

### Architecture
```
ORACLE → opportunities
    ↓
MWGym → experiment task
    ↓
HARBOR → reproducible environment
    ↓
┌──────────┬──────────┬──────────┐
│ Letta    │ Built-in │ Direct/  │
│ Worker   │ agents   │ Pydantic │
└──────────┴──────────┴──────────┘
    ↓
Git workspace + tests
    ↓
HARBOR REWARDKIT
    ↓
WorkerKit receipt
    ↓
Hydra + Git
```

## What to Do Next

### Immediate (today)
1. Install real Harbor: `uv tool install harbor`
2. Create 10 real technical submission tasks
3. Run `harbor run` with real Letta worker
4. Track real costs via TelemetryStore
5. Record in LabProjection

### This week
1. Fix Letta timing (use async/background)
2. Wire real Harbor CLI (not MockHarbor)
3. Run 5-arm crossover with real tasks
4. Test Letta persistent memory across runs
5. Produce report with REAL results only

### This month
1. Build AssetProfile from real outcomes
2. Train StackOracle on real task families
3. Test transfer: does Harbor-learned allocation help on new tasks?
4. Implement promotion gates with real metrics

## Key Files
```
mwgym/
├── HARBOR-PLAN.md          # Authoritative spec
├── HONEST-ASSESSMENT.md    # YGO is wrong testbed
├── STACK-REVIEW.md         # What we have, what's missing
├── PEER-REVIEW-2026-08-31.md  # Comparison with existing work
├── harbor_tasks.py         # 10 Python coding tasks
├── run_harbor_crossover.py # 3-arm experiment runner
├── mwgym/
│   ├── telemetry_records.py  # ModelCall, ResourceSpend
│   ├── asset_profile.py      # Beta posteriors
│   ├── stack_oracle.py       # Thompson allocator
│   ├── compute_wallet.py     # Multi-source budget
│   ├── decision_features.py  # 11 domain features
│   ├── meta_actions.py       # 11 meta-actions
│   ├── lab_bridge.py         # SQLite projection
│   ├── hydra_bridge.py       # Real HydraDB
│   └── harnesses/
│       ├── real_letta.py     # Real Letta adapter
│       └── router.py         # BATS router
```

## Git History
```
bac939e Harbor integration + honest assessment
be591a0 Honest assessment
6526236 YGO-003 results
0d4f43f YGO-002 results
ee581f6 YGO-001 stack wiring
```

## The One-Line Summary

**MWGym has infrastructure but no experiments. The next agent should wire Harbor for real coding tasks and produce results that actually matter for Moltwork.**
