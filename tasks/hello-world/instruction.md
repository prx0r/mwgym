Write a Python function `hello()` that returns the string "Hello, World!".

Requirements:
- Function must be named `hello`
- Must return exactly "Hello, World!"
- No arguments needed
- No imports needed

Example:
```python
result = hello()
assert result == "Hello, World!"
```
