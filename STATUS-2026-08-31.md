# MWGym Status — 2026-08-31 12:00 UTC

## Completed Milestones

### M0 — Repository State Reproducible ✓
- WorkerKit SHA pinned: e625d99
- MWGym SHA pinned: 1c679f5
- upstreams.lock.json created
- All experiments labeled SYNTHETIC

### M1 — Telemetry ✓
- ModelCallRecord, RetrievalRecord, RolloutRecord, ResourceSpend
- TelemetryStore with validation (0 errors on smoke test)
- Costs tracked as UNKNOWN or free_tier (not coerced to zero)

### M2 — Letta Fixed ✓
- Removed hardcoded `stateless: true`
- State comes from `genomeConfig.memory_mode`
- MaxSteps comes from `genomeConfig.max_steps`
- ReasoningEffort comes from `genomeConfig.reasoning_effort`
- 4 treatment arms now possible: L0-L3

### M3 — Hydra Fixed ✓
- LabBridge labeled as SQLite degraded projection
- HydraBridge has degraded flag and backend label
- Real Hydra writes go through HydraBridge

### M4 — Allocator Naming ✓
- Current threshold router labeled ThresholdBudgetAllocator
- BATS name reserved for real BATS semantics

### M5 — Filesystem Smoke ✓
- 3 tasks with full telemetry, 0 validation errors
- Costs, tokens, DecisionPoints all tracked

### M6 — Real YGO World ✓
- ygo-agent cloned (commit dbf5142)
- world.lock.json created
- Pending: card_database_sha256, script_tree_sha256, etc.

## Remaining Work

### M7 — YGO-001 (BLOCKED)
Requires:
1. ygoenv installed (C++ compilation needed)
2. Frozen BasePolicy (hash verified)
3. Real HydraDB writes working
4. DecisionFeatureExtractor
5. Meta-action executor

### M8 — Post-YGO-001
- YGO-002: Memory value
- YGO-003: BATS/resource allocation
- YGO-004: Transfer
- YGO-005: Empirical oracle distillation

## What We Learned

1. **The spec is correct** — we were building "make YGO run through components with right names" instead of testing the architecture
2. **Threshold ≠ BATS** — our "BATS" is a threshold heuristic, not the real method
3. **SQLite ≠ HydraDB** — must be explicitly labeled degraded
4. **stateless:true defeats learning** — was hardcoded, now configurable
5. **SYNTHETIC ≠ REAL** — must label runtime class for scientific validity

## Next Steps

1. Install ygoenv (C++ compilation or pre-built binaries)
2. Freeze BasePolicy with hash verification
3. Build DecisionFeatureExtractor
4. Build meta-action executor
5. Run YGO-001 stack wiring experiment
6. Produce first clean report with REAL runs only
